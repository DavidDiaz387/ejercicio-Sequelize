<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genero y sus Peliculas</title>
</head>
<body>
    <h1><%= genre.name %></h1>
    <ul>
        <% if (genre.Movie) { %>
        <% genre.Movie.forEach(movies => { %>
            <li><%= movies.title %></li>
        <% })} %>

    </ul>
</body> 
</html>